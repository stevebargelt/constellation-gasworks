{
  "name": "@constellation/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@constellation/theme": "workspace:*",
    "@constellation/types": "workspace:*",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^4.1.3"
  }
}
